query JUnitQuery($simpleCriteria: SimpleArgumentInput!) {
  enumQuery
  scalarQuery {
    count
    custom
    id
    name
    rating
    valid
  }
  listQuery {
    id
    name
  }
  complexObjectQuery {
    id
    name
    optional
    details {
      id
      flag
      value
    }
  }
  includeComplexObjectQuery: complexObjectQuery @include(if: true) {
    id
  }
  skipComplexObjectQuery: complexObjectQuery @skip(if: false) {
    id
  }

  interfaceQuery {
    __typename
    id
    name
    ... on FirstInterfaceImplementation {
      intValue
    }
    ... on SecondInterfaceImplementation {
      floatValue
    }
  }
  unionQuery {
    __typename
    ... on BasicObject {
      id
      name
    }
    ... on ComplexObject {
      id
      name
      optional
      details {
        id
        flag
        value
      }
    }
  }
  inputObjectQuery(criteria: $simpleCriteria)
}
