query NestedQuery {
  nestedObjectQuery {
    id
    name
    children {
      name
      children {
        id
        name
        children {
          id
          name
        }
      }
    }
  }
}
